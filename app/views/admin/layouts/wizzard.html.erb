<!DOCTYPE html>
<html>
  <head>
    <title>Faites les comptes ! Administration</title>
    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
  </head>
  <body class='<%= "admin_#{controller.controller_name}" %>  admin'>
    <div class="container-fluid">

      <div class="navbar">
        <div class="navbar navbar-inner">
          <div class="container">
            <a class="brand">Faites les comptes : wizzard de création</a>
            <%= render 'layouts/upper_menu' %>
          </div>
        </div>
      </div>
      <div class="row-fluid">

        <%= render 'layouts/flash_partial' %>

        <%= yield %>
      </div>
    </div>
  
  </body>
</html>
