= content_for :menu do
  = icon_to 'nouveau.png', new_admin_organism_period_nature_path(@organism, @period)

.champ#index
  %h3 Natures du livre #{@book.title}  
  %table.table.table-nature.table-striped.table-bordered.table-condensed(id="#{@book.title.parameterize}" cellpadding="0" cellspacing="0" border="0"  )
    = render :partial=>'table_natures', :locals=>{:list=>@book.natures.within_period(@period).order(:position)}