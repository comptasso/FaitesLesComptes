= content_for :menu do
  = icon_to 'nouveau.png', new_admin_organism_period_nature_path(@organism, @period)

.champ#index
  - @books.each do |book|
    %h3 Natures du livre #{book.title} 
    %table.table.table-striped.table-bordered.table-condensed#recettes(cellpadding="0" cellspacing="0" border="0"  )
      = render :partial=>'table_natures', :locals=>{:list=>book.natures.within_period(@period)}