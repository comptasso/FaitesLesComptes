<%#
# To change this template, choose Tools | Templates
# and open the template in the editor.
%>

<%= content_for :menu do %>
 <%= icon_to 'navigation90.png', admin_organism_period_path(@organism,@period) %>

<% end %>

<fieldset>
  <legend><%= "#{@count} lignes non verrouillées" %> </legend>

   <table class="data_table display">
    <thead>
      <tr>
       <%= render 'lines/table_header' %>
     </tr>
    </thead>
   <tbody id="table_lines">
   
   <% @lines.each do |line| %>
 <%=    content_tag :tr, id: "line_#{line.id}" do %>
      <td class="clickable"><%=l line.line_date %></td>
          <td class="clickable"><%= line.narration %></td>
          <td class="clickable"><%= line.nature.name if line.nature %></td>
          <td class="clickable"><%= line.destination.name if line.destination %></td>
          <td class="nombre clickable"><%= debit_credit(line.debit) %></td>
          <td class="nombre clickable"><%= debit_credit(line.credit) %></td>
          <td class="clickable"><%= line.payment_mode %></td>
          <td class="icon">
      <%=  icon_to 'verrouiller.png', lock_admin_organism_period_line_path(@organism, @period,line),
  :method=>:post, :remote=>true unless line.locked? %></td>
     <% end %>
  
      <% end %>
   </tbody>
 

      </table>

</fieldset>

