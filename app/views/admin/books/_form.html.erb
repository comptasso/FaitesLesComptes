
<%= form_for :book, url: jc_url(@organism,@book), method: jc_method(@book) do |f| %>

  <% if @book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@book.errors.count, "erreurs") %> n'ont pas permis d'enregistrer ce livre : </h2>

      <ul>
      <% @book.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description, rows: 4 %>
  </div>
<div>
  <%= f.label 'Recettes ' %>
  <%=  radio_button "book", :book_type, 'Income', disabled: !@book.new_record? %>
  <%= f.label 'Dépenses ' %>
   <%= radio_button "book", :book_type, 'Outcome', disabled: !@book.new_record? %>
</div>
  <div class="field">
    <%= f.label :image_url %><br />
    <%= f.text_field :image_url %>
  </div>
  <div class="actions">
    <%= f.submit 'Enregistrer' %>
  </div>

  <% end %>