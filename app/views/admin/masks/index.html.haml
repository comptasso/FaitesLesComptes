= content_for :menu do
  = icon_to 'nouveau.png', new_admin_organism_mask_path(@organism)

.champ
  %h3 Liste des masques de saisie d'écritures

%table.table.table-striped.table-bordered.table-condensed(cellpadding="0" cellspacing="0" border="0")
  %thead
    %tr
      %th Title
      %th Comment
      %th Actions
  %tbody
    - @masks.each do |mask|
      %tr
        %td= mask.title
        %td= mask.comment
        %td
          = icon_to 'detail.png', admin_organism_mask_path(@organism, mask)
          = icon_to 'modifier.png', edit_admin_organism_mask_path(@organism, mask)
          

