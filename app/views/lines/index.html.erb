<%= content_for :menu do %>
<%=  icon_to 'printer.png', book_lines_path(@book, mois: @mois, :format=>'pdf') %>
<%=  icon_to 'report-excel.png', book_lines_path(@book, mois: @mois, :format=>'csv'), title: 'CSV' %>
  
<% end -%>

<div class="champ">

  <h3><%= @book.title %> : liste des écritures 
  <%= render 'submenu_mois'  %>
</h3>
 
  </div>
  <div class="row-fluid">
  <table cellpadding="0" cellspacing="0" border="0" class="table table-striped table-bordered table-condensed data_table" >
    <thead>
      <tr>
        <%= render 'table_header' %>
      </tr>
    </thead>
   <tbody id="table_lines">
      <%= render 'table_lines'  %>
   </tbody>
   <tfoot id="table_foot">
     <%= render 'table_foot' %>
   </tfoot>
  </table>
  </div>
  