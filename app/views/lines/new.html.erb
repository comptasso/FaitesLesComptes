<%= content_for :menu do %>

<%= icon_to 'retour.png', book_lines_path(@book) %>
<% end -%>

<%= content_for :submenu do %>
<%= link_to 'Ecritures répétées', new_book_multiple_line_path(@book) %>
<% end %>

<fieldset>
  <legend>
    <%=  @book.title.capitalize %> : nouvelle ligne
  </legend>

<%= render 'form' %>
</fieldset>