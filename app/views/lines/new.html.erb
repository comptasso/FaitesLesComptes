<%= content_for :menu do %>

<%= icon_to 'retour.png', listing_lines_path(@listing) %>
<% end -%>

<%= content_for :submenu do %>
<%= link_to 'Ecritures répétées', new_listing_multiple_line_path(@listing) %>
<% end %>

<fieldset>
  <legend>
    <%=  @listing.title.capitalize %> : nouvelle ligne
  </legend>

<%= render 'form' %>
</fieldset>