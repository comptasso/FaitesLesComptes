- if current_user?
  = current_user.name
  = link_to 'Logout', session_path(current_user), :method=>:delete
- else
  = link_to 'Login', new_session_path

= ActiveRecord::Base.connection_config[:database]
= " org_db : #{session[:org_db]}"
= " period_id : #{session[:period]}"