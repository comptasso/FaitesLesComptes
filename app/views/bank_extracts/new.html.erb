<%= content_for :submenu do %>

<%= icon_to 'navigation90.png', book_bank_extracts_path(@book) if @book.bank_extracts.count > 0 %>
<%= icon_to 'navigation90.png', organism_books_path(@organism) unless @book.bank_extracts.count > 0 %>
<% end -%>

<fieldset>
  <legend>
    Création d'un extrait de compte
  </legend>

<%= render 'form' %>
</fieldset>