<%= content_for :menu do %>

<% end %>

<%= content_for :submenu do %>
<%= icon_to 'nouveau.png', new_book_bank_extract_path(@book) %>
<%= icon_to 'navigation90.png', organism_books_path(@organism) %>
<% end -%>

<fieldset><legend> Liste des extraits de comptes - Journal : <%= @book.title %> </legend>
<table>
  <tr>

  <%= render 'table_header' %>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @bank_extracts.each do |bank_extract| %>
  <tr>

    <%= render :partial=>'bank_extract' , :object=>bank_extract %>
    <td><%= icon_to 'afficher.png', [@book, bank_extract] %></td>
    <td><%= icon_to 'modifier.png', edit_book_bank_extract_path(@book,bank_extract) %></td>
    <td><%= icon_to 'supprimer.png', [@book,bank_extract], :confirm => 'Etes vous sur ?', :method => :delete %></td>

    <td><%= link_to 'Pointage', pointage_path(bank_extract) unless bank_extract.locked? %> </td>
  </tr>
<% end %>
</table>

</fieldset>


