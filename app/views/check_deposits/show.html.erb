
<%= content_for :menu do %>

<%= icon_to 'nouveau.png', new_bank_account_check_deposit_path(@bank_account) if @nb_to_pick > 0 %>
<%= icon_to 'modifier.png', edit_bank_account_check_deposit_path(@bank_account,@check_deposit) %>
<%= icon_to 'navigation90.png', bank_account_check_deposits_path(@bank_account) %>
<% end -%>

<fieldset><legend>Remise de chèques : liste des chèques</legend>
 <%= render 'title' %>

   <table   id="check_added">
 <%= render 'thead' %>
     <tbody>
           <%  @check_deposit.checks.each do |check |%>
      <tr>
     <%= render partial: 'line', object: check %>
      </tr>
    <% end %>

     </tbody>
  <%= render partial: 'tfoot', :object=>@check_deposit.total_checks %>

</table>

</fieldset>

