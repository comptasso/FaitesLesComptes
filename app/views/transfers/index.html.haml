= content_for :menu do
  = icon_to 'navigation90.png',:back
  = icon_to 'nouveau.png', new_organism_transfer_path(@organism)



.champ
  %h3 Liste des transferts

  %table.table.table-striped.table-bordered.table-condensed.data_table#transfer-table(cellpadding="0" cellspacing="0" border="0")
    %thead
      %tr
        %th.date-euro Date
        %th Libellé
        %th.numeric Montant
        %th De
        %th Vers
        %th Actions

    %tbody
      - @transfers.each do |transfer|
        %tr
          %td= l transfer.date
          %td= transfer.narration
          %td.numeric= two_decimals transfer.amount
          %td= transfer.debitable.long_name
          %td= transfer.creditable.long_name

          %td
            = icon_to 'modifier.png', edit_organism_transfer_path(@organism, transfer)
            = icon_to 'supprimer.png', [@organism, transfer], :confirm => 'Are you sure?', :method => :delete



