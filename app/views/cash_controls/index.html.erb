<fieldset>
  <legend>Liste des contrôles de caisse <%= @cash.name %> </legend>
  <table>
    <thead>
      <tr>
    <th>Date</th><th>Montant relevé</th><th>Montant théorique</th><th>Actions</th>
      </tr>
    </thead>
    <tbody>
      <% @cash_controls.each do |r| %>
      <tr>
        <td><%=l  r.date %></td><td class="numeric"><%=  two_decimals r.amount %></td>
        <td><%= @cash.sold(r.date) %></td><td></td>
      </tr>
      <% end %>
    </tbody>
  </table>
</fieldset>
