=content_for :menu do
  = icon_to 'pdf.png', bilan_compta_period_sheets_path(@period, :format=>:pdf)

.champ
  %h3 Bilan simplifié

.test
  %table
    %tbody
      - @total_general.collection.each do |tgl|
        = render :partial=>'line', :collection=>tgl.lines
        = render :partial=>'total_line', :object=>tgl.totals
      = render :partial=>'total_line', :object=>@total_general.totals

