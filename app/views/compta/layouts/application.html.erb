<!DOCTYPE html>
<html>
  <head>
    <title>Faites les comptes ! Compta</title>
    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
  </head>
  <body class='<%=  "compta_#{controller.controller_name}" %> compta'>
      <div class="container-fluid">


  <div class="navbar">
    <div class="navbar navbar-inner">
      <div class="container">
    <a class="brand"><%= header_title %></a>
    <ul class="nav nav-pills pull-right">
    
    <li><%= link_to 'Saisie/Consult',  ((@organism && !@organism.new_record?) ? organism_path(@organism) : organisms_path )  %></li>
    <li><%= link_to 'Administration',  ((@organism && !@organism.new_record?) ? admin_organism_path(@organism) : admin_organisms_path )  %></li>
     <li class="active"><a href="#">Compta</a></li>
    </ul>
  </div>
  </div>
   </div>
 <div class="row-fluid">

  <div>
    <%= render 'compta/menus/menu' %>
  </div>

 <%= render 'layouts/flash_partial' %>


  

  <%= yield %>
  </div>

    </div>
</body>
</html>
