<%#
# To change this template, choose Tools | Templates
# and open the template in the editor.
%>

 <tr class="<%= cycle('list_line_odd', 'list_line_even') %>">
    <td><%= image_tag(listing.image_url, class: 'list_image') %></td>
    <td class="list_description" >
      <dl>

    <dt><%= listing.title %></dt>
    <dd><%= truncate(strip_tags(listing.description), length: 80)  %></dd>
      </dl>
    </td>
    <td class="list_actions"><%= link_to 'Show', [@organism,listing] %>
<br/>
<%= link_to 'Edit', [@organism, listing] %><br/>
    <%= link_to 'Destroy', [@organism,listing], confirm: 'Are you sure?', method: :delete %>
    </td></tr>
