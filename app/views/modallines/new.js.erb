$('div.modal-body').html('<%= j render partial:'form' %>');

// on met en place les actions lors de l affichage
$f_modal_book_change();
if ($('#form_bank_extract_line #line_payment_mode').val() !== 'Chèque') {
  $('#form_bank_extract_line #td_check_number').hide();
}


$('.input_date').datepicker(
    {
        dateFormat: 'dd/mm/yy',
        buttonImage: '/assets/cal.gif',
        buttonImageOnly: true,
        showOn: 'both',
        minDate: $(this).attr('data-jcmin'),
        maxDate: $(this).attr('data-jcmax')
    }
);
