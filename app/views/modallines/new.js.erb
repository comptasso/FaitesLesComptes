$('div.modal-body').html('<%= j render partial:'form' %>');

// on réinitialise les actions de séléction des types de natures en fonction du livre 
// et de l'affichage ou non du mode de paiement.

$f_modal_book_change();
$f_modal_payment_mode_change();

// il faut également réinitialiser le datepicker
$('.input_date_picker').datepicker(
    {
        dateFormat: 'dd/mm/yy',
        minDate: $(this).attr('data-jcmin'),
        maxDate: $(this).attr('data-jcmax')
    }
);
